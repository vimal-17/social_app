<%= form_with(model: group_user) do |form| %>
  <% if group_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_user.errors.count, "error") %> prohibited this group_user from being saved:</h2>

      <ul>
        <% group_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
